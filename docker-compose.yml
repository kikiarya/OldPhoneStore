services:
  web:
    build: .
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
      NODE_ENV: production
      DATABASE_PATH: /app/data/phones.db
    volumes:
      - phone-data:/app/data
    restart: unless-stopped

volumes:
  phone-data:
